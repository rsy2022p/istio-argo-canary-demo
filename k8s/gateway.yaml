apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: product-gateway
  namespace: canary-demo
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - product