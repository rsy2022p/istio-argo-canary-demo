apiVersion: v1
kind: Service
metadata:
  name: product-stable
  namespace: canary-demo
spec:
  selector:
    app: product
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
